# Select LMS Model

Select an LM Studio model from a predefined list.

Provides a dropdown of model identifiers loaded from a `models.txt` file.

Add your favorite model names (one per line) to `comfyui-y7nodes/lms_config/models.txt`.

Output:

  - `model_id`: The selected model identifier string
